import { FarmTracker } from ".";

export namespace Auth {

    export type Login = FarmTracker.components['schemas']['Login']
    export type Response = FarmTracker.components['schemas']['LoginResponse']

}